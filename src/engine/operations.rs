#[derive(Debug, Clone, Copy, PartialEq)]
pub enum ASexualOperation
{
    Relu,
    Tanh,
    Neg
}

#[derive(Debug, Clone, Copy, PartialEq)]
pub enum SexualOperation
{
    Add,
    Sub,
    Mul,
    Div
}
